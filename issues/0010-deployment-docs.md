# Deployment docs & Docker/Heroku instructions

**Summary**
Add `DEPLOYMENT.md` with deployment recipes for Streamlit Cloud (optional), Docker, and Heroku; note required data files and troubleshooting tips.

**Why**
Clear deployment documentation enables team members and users to deploy the application in various environments without extensive DevOps knowledge.

**Acceptance criteria**
- `DEPLOYMENT.md` added with Streamlit/Heroku/Docker instructions and required files list
- Example `Dockerfile` and `Procfile` or templates included

**Labels:** `docs`, `infra`, `devops`
